<!DOCTYPE html>
<html>
<head>
  {% if title %}
   <title> {{title}} -JungleBook </title>
  {%else%}
   <title> JungleBook </title>
  {% endif %}
    <link href="/static/css/bootstrap.min.css" rel="stylesheet" media="screen">
    <link href="/static/css/bootstrap-responsive.min.css" rel="stylesheet">
    <script src="http://code.jquery.com/jquery-latest.js"></script>
    <script src="/static/js/bootstrap.min.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <div>
  {%if g.user.is_authenticated() %}
  {{g.user.first_name}} {{g.user.last_name}} <a href="/">Home</a> |   
  <a href="/profile/{{g.user.id}}">Profile</a> 
  <a href="/logout" style= "float: right;" >Logout</a> <br>
  <form action="{{url_for('search')}}" method='post' name='search'>
  {{g.search_form.hidden_tag()}} Search {{g.search_form.search(size=20)}} </form>
  {% else %}
  JungleBook: <a href="/">Home</a> |
  <a href="/login">Login</a> 	 <a href="/signup" style= "float: right;" >Sign Up</a>
  {% endif %}  </div> <hr>


  {%with messages=get_flashed_messages() %}
  {%if messages%}
  <ul>
  {% for i in messages %}
	<li>{{i}}</li>
  </ul>
  {%endfor%}
  {%endif%}
  {%endwith%}
  {% block content %}{% endblock %}
</body>
</html>
