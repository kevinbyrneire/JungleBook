{%extends 'base.html' %}
{% block content %}

<h1 style = "margin-left:10px;"> {{user.first_name}} {{user.last_name}} - Friends </h1>
<div style = "margin-left:20px;">
  {% with friends=user.list_friends() %}
  {%if friends %}
    <ul>
    {% for f in friends %}
    <li><a href="/profile/{{f.id}}">{{f.first_name}} {{f.last_name}} </a> <br></li>
    {%endfor%}
    </ul>
  {%elif user is g.user%} You don't have any friends yet. <a href = "{{url_for('home')}}" >Find Friends </a>
  {%else%} No Friends
  {%endif%}
  {%endwith%}
</div>

{% endblock %}
